/**
 * @param {Array} existingCoefficientArray
 //  * @param {Number} bondedDots
 
//  * @param {Boolean} editable
//  * @param {Boolean} bonded
//  * @param {Bolean} bonding
//  * @param {Number} parentWidth // in pixels
//  * @param {Number} parentHeght // in pixels
    * @param {Array} axesOptions
    * @param {Number} dotMaxSupply
    * @param {Number} zoomFit
 */
export function ProviderCurve(
  existingCoefficientArray,
  bondedDots,
  // bonded,
  // bonding,
  // editable,
  // parentWidth,
  // parentHeight,
  // axesOptions,
  // dotMaxSupply,
  // zoomFit
) {
  this.existingCoefficientArray = existingCoefficientArray;
  this.bondedDots = bondedDots;
  // this.bonded = bonded;
  // this.bonding = bonding;
  // this.editable = editable;
  // this.parentWidth = parentWidth;
  // this.parentHeight = parentHeight;
  // this.axesOptions = axesOptions;
  // this.dotMaxSupply = dotMaxSupply;
  // this.zoomFit = zoomFit;
}
